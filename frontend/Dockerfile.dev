FROM node:20-alpine

WORKDIR /app

RUN apk add --no-cache libc6-compat caddy

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

ENV NEXT_TELEMETRY_DISABLED=1

EXPOSE 80
CMD ["sh", "-c", "caddy start --config /app/Caddyfile && exec npm run dev"]
