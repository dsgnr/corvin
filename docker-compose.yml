services:
  app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/data
      - ./downloads:/downloads
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
